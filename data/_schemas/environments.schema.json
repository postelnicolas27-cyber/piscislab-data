{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "PiscisLab – Environments schema",
  "description": "Schéma de validation pour les milieux (données publiques, lecture seule).",
  "type": "object",
  "required": [
    "meta",
    "items"
  ],
  "properties": {
    "meta": {
      "type": "object",
      "required": [
        "project",
        "type",
        "version",
        "readonly"
      ],
      "properties": {
        "project": {
          "type": "string",
          "const": "PiscisLab"
        },
        "type": {
          "type": "string",
          "const": "environments"
        },
        "version": {
          "type": "string"
        },
        "readonly": {
          "type": "boolean",
          "const": true
        }
      },
      "additionalProperties": false
    },
    "items": {
      "type": "array",
      "items": {
        "type": "object",
        "required": [
          "id",
          "label"
        ],
        "properties": {
          "id": {
            "type": "string",
            "description": "Identifiant unique du milieu"
          },
          "label": {
            "type": "string",
            "description": "Nom du milieu (ex. rivière, lac, étang, mer)"
          }
        },
        "additionalProperties": false
      }
    }
  },
  "additionalProperties": false
}